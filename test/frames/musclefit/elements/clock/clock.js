'use strict';
import {Element} from '/WolfpackUI/routing/element.js'
export default class Clock extends Page{
    constructor(basePath, framename, elementname){
        super(basePath, framename, elementname);
    }
    onLoaded(){
        super.onLoaded();
    }
}